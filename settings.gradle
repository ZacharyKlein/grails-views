// include "views-core", "views-json"
include 'core', "json", "react", 'markup', 'gradle', 'functional-tests','functional-tests-plugin', 'docs', 'json-templates'
rootProject.children.each { it.name = "views-${it.name}" }